name: PR unit test

on:
  push:


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: ls ${{ github.workspace }}
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.x
          cache: true
      - run: dotnet --version